<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:table="sap.ui.table"
    xmlns:mvc="sap.ui.core.mvc"
>
    <Dialog
        id="invoiceDetailsDialog.id"
        title="{i18n>InvoiceDetails}"
        draggable="true"
        resizable="true"
        contentWidth="80%"
        contentHeight="90%"
        class="sapUiContentPadding"
    >
         
             <Select
                id="payMethodChange.id"
                selectedKey="{dialogState>/PayMethodSelectedKey}"
                width="30%"
                change="onSelectionChange"
                class="sapUiSmallMarginTop sapUiSmallMarginBottom sapUiSmallMarginBegin"
            >
                <core:Item
                    key="OPTION_Full"
                    text="Full"
                />
                <core:Item
                    key="OPTION_Partial"
                    text="Partial"
                />
            </Select>
            
        

        <Table
            id="invoiceTable"
            items="{filtered>/results}"
            width="100%"
        >
            <headerToolbar>
                <Toolbar>
                    <Title text="{filtered>Lifnr}" />
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column>
                    <header>
                        <Label text="{i18n>Catagory}" />
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="{i18n>DocumentNo}" />
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="{i18n>InvoiceDate}" />
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="{i18n>DocumentAmount}" />
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="{i18n>FullPayment}" />
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="{i18n>ApprovalAmount}" />
                    </header>
                </Column>
                <Column>
                    <header>
                        <Label text="{i18n>Project}" />
                    </header>
                </Column>
            </columns>

            <items>
                <ColumnListItem type="Active">
                    <cells>
                        <Text text="{filtered>Category}" />
                        <Text text="{filtered>Docnr}" />
                        <Text text="{filtered>Budat}" />
                        <Text text="{filtered>DocAmt}" />
                        <!-- <Text text="{= ${/fullPayment} ? '✔' : ''}" />
                        <Input editable="{/editable}" value="{ApprovalAmt}" placeholder="partial amount"></Input> -->
                        <Text text="{filtered>Project}" />
                        <Text text="{filtered>Project}" />
                        <Text text="{filtered>Project}" />
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
       
        <beginButton>
            <Button
                text="{i18n>Confirm}"
                type="Emphasized"
                press="onConfirmDialog"
            />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>Cancel}"
                press="onCloseInvoiceDialog"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
